本次仅修改 **app-medication 应用的单一主页面**（原有用药提醒页面），不改动其他页面，严格按本修订版规范重构，重点调整页面布局顺序（提醒卡片置顶，拍照按钮置下），方便老人操作。

核心原则：大字、高对比、少元素、无冗余，适配老人使用，与用药提醒功能严格匹配，所有尺寸、颜色延续原有规范，仅调整布局顺序。

## 一、整体页面规范（核心：单一页面，无跳转，布局顺序调整）

- 页面布局：上下结构，**最顶部为「今日提醒」提示文字+提醒卡片列表**，中间为拍照按钮，**最底部为原有公共按钮**（返回应用中心、打开监控中心），无其他页面跳转；核心约束：公共按钮必须固定在页面最底部，始终可见，无论提醒卡片多少、是否滑动，均不被遮挡，确保老人随时能找到公共按钮

- 页面背景：纯浅灰色 #F5F5F5，无任何渐变、纹理、装饰性图案

- 边距规范：页面左右边距 32px，上下边距 24px，元素之间间距 24px（避免拥挤，方便老人视觉区分）

- 无多余元素：不添加任何装饰图标、分割线（除核心内容外），禁止广告、水印，仅保留指定提示文字


## 二、核心元素设计规范（重点调整布局顺序）

### 2.1 今日提醒提示文字（顶部，优先展示）

- 位置：页面最顶部，提醒卡片列表**左侧**，与页面顶部间距 24px

- 字体：鸿蒙默认字体，34px，黑色 #333333，加粗，无斜体、无下划线

- 样式：仅纯文字”今日提醒”，无背景、无边框、无装饰，与第一张提醒卡片间距 10px（贴合要求，不太远）


### 2.2 提醒卡片（核心展示，位于今日提醒下方）

卡片样式：圆角 24px，白色背景 #FFFFFF，无阴影、无边框，单张卡片高度 180px（适配新增内容），宽度与页面适配（左右留边后全屏）

卡片内部布局（严格按要求排列，清晰有序）：

- 提醒时间：左上位置，字体 35px，黑色 #333333，加粗，无多余修饰

- 提醒状态：右上位置，与提醒时间水平对齐，字体 32px，未提醒=绿色 #34A853，已提醒=灰色 #9E9E9E，加粗

- 药品图片：左下位置，尺寸 80px×80px，居中裁剪，无圆角、无边框、无滤镜，与提醒时间垂直对齐
    
- 药品名称：右下位置，与药品图片水平对齐，字体 34px，黑色 #333333，无修饰，与药品图片间距 10px（不要太远）
    
- 服用剂量：药品名称正下方，字体 25px，颜色 #757575（浅灰色），无修饰，与药品图片间距 10px（不要太远）
    
- 底部提示文字（新增，属于卡片内部元素）：卡片最底部，居中对齐，字体 14px，颜色 #9E9E9E（灰色），纯文字“长按可删除此提醒”，无背景、无边框，确保不出现换行，属于每个提醒卡片的固有内容，每张卡片均需显示（缩小后确保能正常显示，解决不显示问题）
    

卡片间距：单张卡片占满宽度，多张卡片上下排列，间距 24px，无横向排列（避免老人左右滑动）；有多条提醒时，支持上下滑动查看，滑动无卡顿、无动画，滑动区域为提醒卡片列表整体，适配老人操作。

无提醒卡片时展示：当无任何用药提醒时，显示提示文本“今日还没有提醒”，该文本与“今日提醒”提示文字间距 20px，居中显示，字体 40px，黑色 #333333，无背景、无边框、无装饰，与页面整体风格一致。

### 2.3 拍照按钮（核心操作，位于提醒卡片下方）

- 尺寸：直径 120px（固定），圆角 60px（全圆角，无棱角）

- 颜色：主色 #FF6B6B（珊瑚红，无渐变、无阴影），点击时变色为 #E55353（深一点的珊瑚红），松开恢复原色

- 图标：药品图标 💊（尺寸 60px），居中放置，无任何文字、边框

- 位置：提醒卡片列表下方，居中放置，与上方提醒卡片列表间距 24px，与下方公共按钮间距根据底部预留空间自适应

- 交互反馈：点击时震动 50ms + 颜色变化，无其他弹窗、动画

- 操作说明：
  - 点击：打开相册选择药品图片，选图后语音询问"一天吃几次，一次吃多少"
  - 长按（0.5秒）：直接触发语音识别，说药品名+时间+频率，自动创建提醒
  - 语音识别中点击：取消语音识别，播报"已取消"

- 操作便利性：置下放置方便老人手指操作，单手可及


### 2.4 公共按钮（原有，位置调整至最底部）

- 位置：页面最底部，固定布局（不随页面滑动而移动），居中/按原有排列方式放置，与上方提醒卡片列表间距 48px；核心要求：公共按钮始终悬浮/固定在底部，不被提醒卡片、滑动区域遮挡，确保滑动到任何位置都能看见
    
- 样式：保持现有颜色、布局不变，仅将公共按钮文本字号缩小30%（按原有默认字号缩小，适配整体缩小风格），大小与页面其他缩小后文字协调，确保老人清晰可见；其余样式、大小、功能不变，仅调整位置至底部，确保与新页面风格协调（不突兀）
    
- 交互：点击反馈与现有项目一致，不新增任何额外交互，不修改原有功能
    

## 三、字体与颜色规范（延续原有，新增补充）

### 3.1 字体规范

- 全局字体：鸿蒙默认字体（与现有项目一致），无特殊字体、无斜体、无下划线
    
- 字号要求（新增补充）：
    
    - 提醒时间：35px（加粗）
        
    - 今日提醒、药品名称：34px；提醒状态：32px（提醒状态加粗）
        
    - 底部删除提示：14px（颜色更浅）；服用剂量：25px（颜色更浅）；无提醒卡片时的提示文本：40px，黑色 #333333（不缩小，仅卡片内部文字、公共按钮文本缩小）；公共按钮文本：按原有默认字号缩小30%，适配整体风格
        
- 字体颜色（新增补充）：
    
    - 核心文字（时间、今日提醒、药品名称）：纯黑色 #333333
        
    - 未提醒状态：绿色 #34A853，已提醒状态：灰色 #9E9E9E
        
    - 服用剂量：浅灰色 #757575（比药品名称浅）
        
    - 底部删除提示：灰色 #9E9E9E
        
- 对齐方式：提醒时间、药品图片左对齐；提醒状态右上对齐；药品名称、服用剂量右对齐；底部提示文字居中对齐
    

### 3.2 颜色规范（无新增，延续原有，共4种核心颜色）

- 页面背景：#F5F5F5（浅灰）
    
- 拍照按钮：#FF6B6B（珊瑚红，点击后 #E55353）
    
- 提醒卡片：#FFFFFF（白色）
    
- 文字/状态：#333333（黑）、#34A853（绿）、#9E9E9E（灰）、#757575（浅灰，仅用于服用剂量）
    

## 四、交互设计规范（延续原有，补充删除提示和语音交互）

- 拍照按钮：
  - 点击：打开相册选择药品图片，选图后自动语音询问”一天吃几次，一次吃多少”
  - 长按（0.5秒）：直接触发语音识别，说药品名+时间+频率，自动创建提醒
  - 语音识别中点击：取消语音识别，播报”已取消”
  - 反馈：震动 50ms + 颜色变化

- 提醒卡片：点击无任何响应（防误触），长按卡片弹出”删除”提示（提示文字 48px，绿色 #34A853，仅”删除”二字，点击删除）；卡片底部”长按可删除此提醒”仅为提示，不响应点击；提醒卡片删除后，下方的提醒卡片自动顶上去，保持卡片间距 24px不变

- 无任何弹窗：除长按卡片的”删除”提示外，不弹出任何确认、提示弹窗

- 无动画效果：禁止任何过渡动画、加载动画（避免老人眼花）；滑动查看提醒时无卡顿、无过渡动画；滑动区域仅为提醒卡片列表，不影响底部公共按钮，公共按钮始终固定在底部可见
    

## 五、适配规范（延续原有，无修改）

- 屏幕适配：6.1"-6.8" 机型，自适应屏幕宽度，卡片、按钮、文字不拉伸、不压缩
    
- 大字体适配：系统开启大字体模式后，页面所有文字同步放大，不出现文字截断、重叠（重点适配底部提示文字、服用剂量及公共按钮文本，三者已缩小，需确保放大后正常显示）
    
- 分屏适配：分屏状态下，页面元素不错乱，公共按钮（底部固定，始终可见）、拍照按钮（顶部）、提醒卡片、提示文字均正常显示，滑动功能正常，公共按钮不被分屏界面遮挡
    

## 六、禁止规范（绝对不能违反，补充新增约束）

- 禁止添加任何装饰性元素：图标、分割线、渐变、阴影、水印、广告
    
- 禁止使用低对比颜色：如浅灰文字配浅灰背景、浅色按钮配浅色背景（尤其注意服用剂量、底部提示及公共按钮文本，缩小后需确保与对应背景区分清晰，避免看不清）
    
- 禁止缩小字体：严格按字号要求执行，底部删除提示固定为14px，服用剂量固定为25px，提醒时间固定为35px，提醒状态固定为32px，今日提醒、药品名称固定为34px，无提醒卡片时的提示文本固定为40px；公共按钮文本按原有默认字号缩小30%（仅卡片内部文字、公共按钮文本缩小，其余不变）
    
- 禁止修改公共按钮：仅调整位置至底部且设置为固定布局（始终可见），样式、颜色、大小、功能均保持原有状态；禁止设置公共按钮随页面滑动隐藏，确保任何情况下都能看见公共按钮
    
- 禁止多页面跳转、复杂交互：全程仅一个页面，无二级页面、无滑动切换（提醒卡片滑动查看除外）
    
- 禁止药品图片添加边框、圆角、滤镜：保持原图居中裁剪，清晰可见
    
- 禁止修改提醒卡片内部布局：严格按“时间左上、状态右上、图片左下、名称右下（与图片间距10px）、剂量在名称下方（与图片间距10px）、底部提示文字（14px，不换行，属于卡片内部元素，每张卡片均需显示）”排列；禁止修改“今日提醒”与第一张提醒卡片的间距，固定为10px；禁止修改无提醒卡片时“今日还没有提醒”提示文本的样式（40px、居中、与今日提醒间距20px）；禁止修改公共按钮文本字号缩小比例，需按原有默认字号缩小30%
    
- 禁止删除新增元素：必须保留“今日提醒”提示文字、卡片底部“长按可删除此提醒”提示（14px，不换行，属于卡片内部元素，每张卡片均需显示）及无提醒卡片时的“今日还没有提醒”提示文本（40px、居中、与今日提醒间距20px）；禁止关闭提醒卡片列表滑动功能，确保多条提醒可正常上下滑动查看；删除提醒卡片后，下方卡片需自动顶上去，保持间距规范；禁止修改“今日提醒”与第一张提醒卡片10px的间距及与无提醒提示文本20px的间距；禁止设置公共按钮随页面滑动隐藏，必须固定在底部始终可见，不被任何元素遮挡；禁止修改公共按钮文本字号缩小比例，需按原有默认字号缩小30%
    
- 禁止调整布局顺序：严格按”今日提醒+提醒卡片（最顶部）→ 拍照按钮（中间）→ 公共按钮（最底部，固定可见）”排列，不得随意调整；禁止将公共按钮设置为可滑动隐藏，确保始终可见


补充说明：本规范为修订版，优先于现有项目通用设计规范，仅针对 app-medication 本次升级重构的单一主页面，所有设计均围绕”老人能看清、能操作、不困惑”展开，重点强调：公共按钮必须固定在页面最底部，始终可见，无论页面如何滑动、提醒卡片多少，均不被遮挡；拍照按钮位于提醒卡片下方，方便老人手指操作；同时明确”长按可删除此提醒”为每张提醒卡片的内部元素，字体固定14px，每张卡片均需显示（缩小后确保正常显示，解决此前不显示问题）；提醒卡片内部所有文字、公共按钮文本均按原尺寸缩小30%（公共按钮文本按原有默认字号缩小，取整适配），其余文字尺寸不变，需严格对照每一条执行，不符合规范的需重新调整。

---

## 实施执行记录

### 执行时间（最新）
2026-04-02

### 执行状态
**构建成功** ✅

### 本次修订内容（公共按钮文本字号缩小30%）

#### 改动说明
公共按钮文本按原有默认字号缩小30%，适配整体缩小风格：

| 元素 | 原字号 | 新字号（缩小30%取整） |
|------|--------|----------------------|
| 返回应用中心 | 32px | **22px** |
| 打开监控中心 | 32px | **22px** |

#### 代码变更
```typescript
// Index.ets 公共按钮字号调整
Button('返回应用中心').fontSize(22)  // 32 → 22
Button('打开监控中心').fontSize(22)  // 32 → 22
```

#### 验证结果
- 构建状态: **BUILD SUCCESSFUL in 2 s 299 ms**
- 启动状态: **start ability successfully**

---

### 完整字号对照表

| 元素 | 原字号 | 最终字号 | 备注 |
|------|--------|---------|------|
| 提醒时间 | 50px | 35px | 缩小30% |
| 提醒状态 | 45px | 32px | 缩小30% |
| 今日提醒 | 48px | 34px | 缩小30% |
| 药品名称 | 48px | 34px | 缩小30% |
| 服用剂量 | 35px | 25px | 缩小30% |
| 底部删除提示 | 20px | 14px | 缩小30% |
| 公共按钮文本 | 32px | 22px | 缩小30% |
| 无提醒提示文本 | 40px | 40px | 不缩小 |

---

### 前序修订记录

#### 公共按钮固定布局
- Stack 分层布局，公共按钮固定底部始终可见
- 滚动区域独立隔离，底部 padding 120px 预留空间

#### 卡片字体统一缩小30%
- 提醒时间、状态、药品名称、服用剂量、底部提示统一缩小

---

### 2026-04-02 更新：布局顺序调整

#### 改动说明
将拍照按钮与提醒卡片区域位置对调，优化老人操作体验：

| 原布局 | 新布局 | 调整原因 |
|--------|--------|---------|
| 拍照按钮（顶部） | 今日提醒+提醒卡片（顶部） | 提醒信息优先展示 |
| 今日提醒+提醒卡片（中间） | 拍照按钮（中间） | 拍照按钮置下方便手指操作 |

#### 设计理念
- **信息优先**：今日提醒置顶，老人进入应用第一眼看到待办事项
- **操作便利**：拍照按钮置下，单手可及，符合老人手指操作习惯
- **视觉层次**：信息查看在上，操作入口在下，逻辑清晰

#### 代码变更
```typescript
// Index.ets 布局顺序调整
build() {
  Stack({ alignContent: Alignment.Bottom }) {
    Column() {
      // 今日提醒提示（顶部）
      Text('今日提醒')...

      // 卡片列表（可滑动区域）
      Scroll() { ... }

      // 拍照按钮（提醒卡片下方）
      Column() { Button()... }
        .padding({ bottom: 120 }) // 为底部公共按钮预留空间
    }

    // 固定底部公共按钮
    Column() { ... }
  }
}
```

#### 验证结果
- 构建状态: **BUILD SUCCESSFUL in 3 s 996 ms**
- 启动状态: **start ability successfully**

### 2026-04-02 更新：拍照后语音多轮对话设置提醒

#### 改动说明
重构拍照按钮交互流程，实现完全语音化的提醒设置：

| 原流程 | 新流程 |
|--------|--------|
| 拍照 → 时间选择器 UI | 选图 → 语音多轮对话 |
| 手动选择时间 | 语音询问频率，自动生成时间 |
| 手动确认 | 语音播报确认 |

#### 操作流程
```
点击拍照按钮 → 选择药品图片 → 语音询问"请说药品名称"
→ 用户说药品名 → 语音询问"一天吃几次？"
→ 用户说频率 → 语音询问"一次吃多少？"
→ 用户说剂量 → 语音确认设置内容 → 自动创建提醒
```

#### 语音交互规范
- **药品名称**：用户说的内容直接作为药品名称
- **频率解析**：
  - "一天一次" / "一次" → 08:00
  - "一天两次" / "两次" → 08:00, 17:00
  - "一天三次" / "三次" → 08:00, 12:00, 18:00
- **剂量解析**：支持"X片"、"X粒"、"X袋"、"X勺"等表达
- **确认播报**："好的，已设置[药品名]，[频率]，每次[剂量]"

#### 长按功能保留
长按拍照按钮仍可直接语音设置提醒（无需图片）

#### 验证结果
- 构建状态: **BUILD SUCCESSFUL in 3 s 671 ms**

### 2026-04-02 更新：语音识别音频采集修复 + 空提醒提示样式优化

#### 改动说明

**1. 语音识别音频采集修复**

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| ASR session wave is 0 | startListening(audioInfo) 进入外部音频模式，但没有 writeAudio | 使用 AudioCapturer 采集麦克风音频，通过 writeAudio() 写入 ASR |

**技术实现**：
```typescript
// 创建 AudioCapturer 采集麦克风音频
audioCapturer = await audio.createAudioCapturer({
  streamInfo: { samplingRate: 16000, channels: 1, sampleFormat: SAMPLE_FORMAT_S16LE },
  capturerInfo: { source: SOURCE_TYPE_MIC }
});

// 监听音频数据并写入 ASR
audioCapturer.on('readData', (buffer: ArrayBuffer) => {
  this.asrEngine.writeAudio(sessionId, new Uint8Array(buffer));
});
```

**2. 空提醒提示样式优化**

| 元素 | 原样式 | 新样式 |
|------|--------|--------|
| 字体大小 | 40px | **28px**（比"今日提醒"34px 小） |
| 字体颜色 | #333333（黑色） | **#9E9E9E**（灰色，不那么黑） |
| 字体粗细 | Bold | **Normal** |
| 图标 | 无 | **📋**（48px） |

#### 代码变更
```typescript
// Index.ets 空提醒提示
Column({ space: 12 }) {
  Text('📋').fontSize(48)
  Text('今日还没有提醒')
    .fontSize(28)
    .fontColor(COLORS.textHint)  // #9E9E9E
    .fontWeight(FontWeight.Normal)
}
```

#### 验证结果
- 构建状态: **BUILD SUCCESSFUL in 3 s 216 ms**